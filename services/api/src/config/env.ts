import "dotenv/config";
import { z } from "zod";
const schema=z.object({NODE_ENV:z.enum(["development","test","production"]).default("development"),PORT:z.coerce.number().default(4000),WEB_URL:z.string().url().default("http://localhost:3000"),WEB_URLS:z.string().optional(),API_PUBLIC_URL:z.string().url().default("http://localhost:4000"),DATABASE_URL:z.string(),AI_URL:z.string().url().default("http://localhost:8000"),GEMINI_API_KEY:z.string().min(1).optional(),GEMINI_BASE_URL:z.string().url().default("https://generativelanguage.googleapis.com/v1beta"),GEMINI_MODEL:z.string().min(1).default("gemini-2.5-flash-lite"),OPENAI_API_KEY:z.string().min(1).optional(),OPENAI_BASE_URL:z.string().url().default("https://api.openai.com/v1"),VISION_MODEL:z.string().min(1).default("gpt-5.6-luna"),VISION_TIMEOUT_MS:z.coerce.number().int().min(5000).max(120000).default(45000),JWT_ACCESS_SECRET:z.string().min(16).default("local-access-secret-change-me"),JWT_REFRESH_SECRET:z.string().min(16).default("local-refresh-secret-change-me"),EMAIL_TOKEN_SECRET:z.string().min(16).default("local-email-token-secret-change-me"),COOKIE_SECURE:z.string().default("false"),COOKIE_SAME_SITE:z.enum(["lax","strict","none"]).default("lax"),MINIO_ENDPOINT:z.string().default("localhost"),MINIO_PORT:z.coerce.number().default(9000),MINIO_USE_SSL:z.string().default("false"),MINIO_FORCE_PATH_STYLE:z.string().default("true"),MINIO_REGION:z.string().default("us-east-1"),MINIO_ACCESS_KEY:z.string().default("completeit"),MINIO_SECRET_KEY:z.string().default("completeit-local-secret"),MINIO_BUCKET:z.string().default("completeit"),MAIL_HOST:z.string().default("localhost"),MAIL_PORT:z.coerce.number().default(1025),MAIL_SECURE:z.string().default("false"),MAIL_USER:z.string().optional(),MAIL_PASSWORD:z.string().optional(),MAIL_FROM:z.string().default("CompleteIt <no-reply@completeit.local>"),MAIL_PREVIEW_URL:z.string().url().optional()});
export const env=schema.parse(process.env);
