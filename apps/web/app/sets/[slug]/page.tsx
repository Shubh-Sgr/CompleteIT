"use client";

import Link from "next/link";
import {useParams,useRouter} from "next/navigation";
import {FormEvent,useEffect,useState} from "react";
import {useQuery,useQueryClient} from "@tanstack/react-query";
import {CheckCircle2,Copy,Forklift,Heart,MessageCircle,Pencil,Share2,Sparkles,Star,TriangleAlert,UserRoundCheck} from "lucide-react";
import {api,money,timeAgo} from "@/lib/api";
import {SetCard} from "@/components/set-card";

const ratingFields=[["outcome","Outcome achieved"],["value","Value for money"],["compatibility","Compatibility"],["usefulness","Usefulness"],["comfort","Comfort"],["appearance","Appearance"],["durability","Durability"]] as const;
const updateKinds=[["STARTED_USING","Started using"],["STILL_USING","Still using"],["REPLACED","Replaced a product"],["REMOVED_UNNECESSARY","Removed as unnecessary"],["BROKE","A product broke"],["UPGRADED","Upgraded"],["WOULD_PURCHASE_AGAIN","Would purchase again"],["WOULD_NOT_PURCHASE_AGAIN","Would not purchase again"]] as const;

export default function SetDetail(){
  const {slug}=useParams<{slug:string}>(),router=useRouter(),queryClient=useQueryClient();
  const {data,error,isLoading}=useQuery({queryKey:["set",slug],queryFn:()=>api<any>(`/sets/${slug}`)});
  const [notice,setNotice]=useState(""),[busy,setBusy]=useState(""),[visibility,setVisibility]=useState("PRIVATE");
  const [comment,setComment]=useState(""),[suggestionType,setSuggestionType]=useState("FILL_MISSING_SLOT"),[suggestionReason,setSuggestionReason]=useState("");
  const [rating,setRating]=useState<Record<string,number>>(Object.fromEntries(ratingFields.map(([key])=>[key,5]))),[ratingExplanation,setRatingExplanation]=useState(""),[ratingDays,setRatingDays]=useState(30),[ownsSet,setOwnsSet]=useState(false);
  const [updateKind,setUpdateKind]=useState("STILL_USING"),[updateNote,setUpdateNote]=useState(""),[updateDays,setUpdateDays]=useState(30);
  useEffect(()=>{if(data?.set.visibility)setVisibility(data.set.visibility)},[data?.set.visibility]);

  async function request(path:string,method="POST",body?:unknown,success="Done — the set has been updated."){
    setBusy(path);setNotice("");
    try{const result=await api<any>(path,{method,body:body===undefined?undefined:JSON.stringify(body)});setNotice(success);await Promise.all([queryClient.invalidateQueries({queryKey:["set",slug]}),queryClient.invalidateQueries({queryKey:["my-sets"]}),queryClient.invalidateQueries({queryKey:["explore"]})]);return result}
    catch(value){const message=(value as Error).message;if(/auth|session/i.test(message))router.push("/login");else setNotice(message)}
    finally{setBusy("")}
  }

  if(isLoading)return <div className="skeleton h-[70vh]"/>;
  if(error||!data?.set)return <div className="panel mx-auto max-w-lg p-10 text-center"><h1 className="text-2xl font-black">This set isn’t available</h1><p className="mt-3 text-slate-500">It may be private, followers-only, or no longer exist.</p></div>;
  const set=data.set,viewer=data.viewer??{};
  const groupNames=[...new Set<string>([...set.slots.map((value:any)=>String(value.name)),...set.items.map((value:any)=>String(value.slotName||"Other items"))])];
  const groups=groupNames.map(name=>({slot:set.slots.find((value:any)=>value.name===name)??{id:name,name,purpose:"User-defined products",required:false,status:"FILLED"},items:set.items.filter((value:any)=>(value.slotName||"Other items")===name)}));

  async function submitComment(event:FormEvent){event.preventDefault();if(!comment.trim())return;const result=await request(`/sets/${slug}/comments`,"POST",{body:comment},"Comment added.");if(result)setComment("")}
  async function submitSuggestion(event:FormEvent){event.preventDefault();const result=await request(`/sets/${slug}/suggestions`,"POST",{type:suggestionType,reason:suggestionReason},"Suggestion sent to the owner.");if(result)setSuggestionReason("")}
  async function submitRating(event:FormEvent){event.preventDefault();await request(`/sets/${slug}/ratings`,"POST",{...rating,usageDays:ratingDays,ownsSet,explanation:ratingExplanation},viewer.hasRated?"Your rating was updated.":"Rating published.")}
  async function submitUpdate(event:FormEvent){event.preventDefault();const result=await request(`/sets/${slug}/updates`,"POST",{kind:updateKind,note:updateNote,usageDays:updateDays},"Living Set update published.");if(result)setUpdateNote("")}

  return <div className="space-y-10">
    <section className="overflow-hidden rounded-[2rem] bg-ink text-white dark:bg-indigo-950">
      <div className="min-h-64 bg-gradient-to-br from-indigo-700 via-violet-700 to-amber-500 p-7 sm:p-10"><div className="flex flex-wrap gap-2"><span className="chip !border-white/20 !bg-black/20 !text-white">{set.visibility.toLowerCase()}</span><span className="chip !border-white/20 !bg-black/20 !text-white">{set.status.toLowerCase()}</span><span className="chip !border-white/20 !bg-black/20 !text-white">{set.usageDays} days used</span></div><h1 className="mt-10 max-w-3xl text-4xl font-black tracking-tight sm:text-5xl">{set.title}</h1><p className="mt-4 max-w-2xl text-white/80">{set.description}</p></div>
      <div className="grid gap-5 p-6 sm:grid-cols-4"><Stat label="Demo total" value={money(set.totalPrice)}/><Stat label="Slots completed" value={`${Math.round(set.slotScore)}%`}/><Stat label="Compatibility" value={`${Math.round(set.compatibilityScore)}%`}/><Stat label={`Community · ${set.ratings.length} rating${set.ratings.length===1?"":"s"}`} value={`${Number(set.communityRating).toFixed(1)} / 5`}/></div>
    </section>
    {notice&&<div role="status" className="rounded-xl bg-indigo-50 p-4 text-sm font-semibold text-indigo-800 dark:bg-indigo-950 dark:text-indigo-100">{notice}</div>}
    <div className="grid gap-8 lg:grid-cols-[1fr_21rem]">
      <div className="space-y-8">
        <section><p className="eyebrow">Functional audit</p><h2 className="mt-2 text-2xl font-black">Products by slot</h2><div className="mt-5 space-y-3">{groups.map(({slot,items}:any)=><div className="panel p-5" key={slot.id}><div className="flex items-start justify-between gap-3"><div><h3 className="font-bold">{slot.name}</h3><p className="text-xs text-slate-500">{slot.purpose}</p></div>{items.length?<CheckCircle2 className="shrink-0 text-emerald-500"/>:<span className="chip !text-amber-700">Missing {slot.required?"required":"optional"}</span>}</div>{items.map((item:any)=><div key={item.id} className="mt-4 flex items-center justify-between gap-4 border-t pt-4 dark:border-slate-700"><div><strong className="text-sm">{item.product.name}</strong><span className="ml-2 text-xs text-slate-500">{item.owned?"Owned":"Planned"} · {item.product.brand}</span><p className="mt-1 text-xs text-slate-500">{item.attribution}</p></div><b className="text-sm">{money(item.selectedPrice)}</b></div>)}</div>)}</div>{groups.length===0&&<p className="panel mt-5 p-6 text-sm text-slate-500">No products have been added yet.</p>}</section>

        <section><p className="eyebrow">Living Set timeline</p><h2 className="mt-2 text-2xl font-black">What changed over time</h2><div className="mt-5 border-l-2 border-indigo-200 pl-5 dark:border-indigo-900">{set.updates.map((update:any)=><div key={update.id} className="relative mb-6"><span className="absolute -left-[1.72rem] top-1 size-3 rounded-full bg-indigo-600 ring-4 ring-paper dark:ring-[#0e111a]"/><strong className="text-sm">{update.usageDays}-day · {update.kind.toLowerCase().replaceAll("_"," ")}</strong><p className="mt-1 text-sm text-slate-500">{update.note}</p><small className="text-slate-400">{timeAgo(update.createdAt)}</small></div>)}{set.updates.length===0&&<p className="pb-4 text-sm text-slate-500">No usage updates yet.</p>}</div>
          {viewer.isOwner&&<form onSubmit={submitUpdate} className="panel mt-5 grid gap-3 p-5 sm:grid-cols-2"><h3 className="font-bold sm:col-span-2">Publish an outcome update</h3><label><span className="text-xs font-bold text-slate-500">Update type</span><select aria-label="Update type" className="field mt-1 w-full" value={updateKind} onChange={event=>setUpdateKind(event.target.value)}>{updateKinds.map(([value,label])=><option value={value} key={value}>{label}</option>)}</select></label><label><span className="text-xs font-bold text-slate-500">Days used</span><input aria-label="Days used" className="field mt-1 w-full" type="number" min="0" value={updateDays} onChange={event=>setUpdateDays(Number(event.target.value))}/></label><label className="sm:col-span-2"><span className="text-xs font-bold text-slate-500">What changed?</span><textarea aria-label="Update note" required minLength={5} className="field mt-1 min-h-24 w-full" value={updateNote} onChange={event=>setUpdateNote(event.target.value)} placeholder="What stayed useful, changed, broke or was removed?"/></label><button disabled={Boolean(busy)} className="rounded-xl bg-indigo-600 px-4 py-2.5 font-bold text-white sm:col-span-2">Publish update</button></form>}
        </section>

        <section><p className="eyebrow">Community</p><h2 className="mt-2 text-2xl font-black">Suggestions and discussion</h2><div className="mt-4 grid gap-3">{set.suggestions.map((suggestion:any)=><div className="panel p-4" key={suggestion.id}><div className="flex flex-wrap justify-between gap-2"><b className="text-sm">{suggestion.type.toLowerCase().replaceAll("_"," ")}</b><span className="chip">{suggestion.status.toLowerCase()}</span></div><p className="mt-2 text-sm text-slate-500">{suggestion.reason}</p>{suggestion.discussion&&<p className="mt-2 rounded-lg bg-slate-50 p-2 text-xs dark:bg-slate-800">Owner response: {suggestion.discussion}</p>}<small>by @{suggestion.author.username}</small>{viewer.isOwner&&suggestion.status==="OPEN"&&<div className="mt-3 flex flex-wrap gap-2"><button onClick={()=>request(`/suggestions/${suggestion.id}`,"PATCH",{status:"ACCEPTED",discussion:"Accepted by the set owner."},"Suggestion accepted.")} className="chip !text-emerald-700">Accept</button><button onClick={()=>request(`/suggestions/${suggestion.id}`,"PATCH",{status:"DISCUSSING",discussion:"The owner is reviewing this suggestion."},"Suggestion moved to discussion.")} className="chip">Discuss</button><button onClick={()=>request(`/suggestions/${suggestion.id}`,"PATCH",{status:"REJECTED",discussion:"Not a fit for this set right now."},"Suggestion rejected.")} className="chip !text-red-700">Reject</button></div>}</div>)}{set.comments.map((entry:any)=><div className="panel p-4" key={entry.id}><p className="text-sm">{entry.body}</p><small className="mt-2 block text-slate-500">@{entry.author.username}</small></div>)}{set.suggestions.length===0&&set.comments.length===0&&<p className="text-sm text-slate-500">No community activity yet.</p>}</div>
          {!viewer.isOwner&&set.visibility==="PUBLIC"&&<div className="mt-5 grid gap-4 lg:grid-cols-2"><form onSubmit={submitComment} className="panel p-5"><h3 className="flex items-center gap-2 font-bold"><MessageCircle size={17}/>Add a comment</h3><textarea aria-label="Comment" required minLength={2} className="field mt-3 min-h-24 w-full" value={comment} onChange={event=>setComment(event.target.value)} placeholder="Share something useful about this set…"/><button disabled={Boolean(busy)} className="mt-3 rounded-xl bg-indigo-600 px-4 py-2 text-sm font-bold text-white">Post comment</button></form><form onSubmit={submitSuggestion} className="panel p-5"><h3 className="flex items-center gap-2 font-bold"><Sparkles size={17}/>Suggest an improvement</h3><select aria-label="Suggestion type" className="field mt-3 w-full" value={suggestionType} onChange={event=>setSuggestionType(event.target.value)}><option value="ADD_PRODUCT">Add product</option><option value="REMOVE_PRODUCT">Remove product</option><option value="REPLACE_PRODUCT">Replace product</option><option value="CHEAPER_ALTERNATIVE">Use cheaper alternative</option><option value="CORRECT_COMPATIBILITY">Correct compatibility</option><option value="FILL_MISSING_SLOT">Fill missing slot</option></select><textarea aria-label="Suggestion reason" required minLength={10} className="field mt-3 min-h-20 w-full" value={suggestionReason} onChange={event=>setSuggestionReason(event.target.value)} placeholder="Explain why this would improve the outcome…"/><button disabled={Boolean(busy)} className="mt-3 rounded-xl border px-4 py-2 text-sm font-bold">Send suggestion</button></form></div>}
        </section>

        {!viewer.isOwner&&set.visibility==="PUBLIC"&&<details className="panel p-5"><summary className="cursor-pointer font-bold">{viewer.hasRated?"Update your detailed rating":"Rate this set"}</summary><form onSubmit={submitRating} className="mt-5 grid gap-4 sm:grid-cols-2">{ratingFields.map(([key,label])=><label key={key}><span className="text-xs font-bold text-slate-500">{label}</span><select aria-label={label} className="field mt-1 w-full" value={rating[key]} onChange={event=>setRating(current=>({...current,[key]:Number(event.target.value)}))}>{[5,4,3,2,1].map(value=><option value={value} key={value}>{value} / 5</option>)}</select></label>)}<label><span className="text-xs font-bold text-slate-500">Usage duration</span><input aria-label="Rating usage days" type="number" min="0" className="field mt-1 w-full" value={ratingDays} onChange={event=>setRatingDays(Number(event.target.value))}/></label><label className="flex items-center gap-2 pt-6"><input type="checkbox" checked={ownsSet} onChange={event=>setOwnsSet(event.target.checked)}/> I own or recreated this set</label><label className="sm:col-span-2"><span className="text-xs font-bold text-slate-500">Written explanation</span><textarea aria-label="Rating explanation" required minLength={10} className="field mt-1 min-h-24 w-full" value={ratingExplanation} onChange={event=>setRatingExplanation(event.target.value)} placeholder="What outcome did it achieve, and what trade-offs did you observe?"/></label><button disabled={Boolean(busy)} className="rounded-xl bg-indigo-600 px-4 py-2.5 font-bold text-white sm:col-span-2">Publish rating</button></form></details>}
      </div>

      <aside className="space-y-4 lg:sticky lg:top-24 lg:self-start">
        <div className="panel p-5"><p className="text-sm text-slate-500">Created by</p><Link href={`/users/${set.owner.username}`} className="mt-2 flex items-center gap-3"><span className="grid size-10 place-items-center rounded-full bg-indigo-100 font-black text-indigo-700">{set.owner.profile.displayName[0]}</span><span><b className="block">{set.owner.profile.displayName}</b><small className="text-slate-500">@{set.owner.username}</small></span></Link><p className="mt-4 text-sm text-slate-500">Outcome</p><b className="mt-1 block text-sm">{set.outcome}</b><p className="mt-4 text-sm text-slate-500">Budget constraint</p><b className="mt-1 block">{money(set.budget)}</b></div>
        {viewer.isOwner?<div className="panel p-4"><div className="flex items-center gap-2"><UserRoundCheck size={18} className="text-indigo-600"/><b>Owner controls</b></div><label className="mt-4 block text-xs font-bold text-slate-500" htmlFor="set-visibility">Who can view this set?</label><select id="set-visibility" className="field mt-1 w-full" value={visibility} onChange={event=>setVisibility(event.target.value)}><option value="PRIVATE">Private — only me</option><option value="FOLLOWERS">Followers-only</option><option value="UNLISTED">Unlisted — link access</option><option value="PUBLIC">Public — discoverable</option></select><button disabled={Boolean(busy)||visibility===set.visibility} onClick={()=>request(`/sets/${slug}/publish`,"POST",{visibility},visibility==="PUBLIC"?"Set published publicly.":`Visibility changed to ${visibility.toLowerCase()}.`)} className="mt-3 w-full rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-bold text-white disabled:opacity-50">Apply visibility</button><div className="mt-3 grid grid-cols-2 gap-2"><Link href={`/sets/${slug}/edit`} className="flex items-center justify-center gap-2 rounded-xl border p-2.5 text-sm font-bold"><Pencil size={16}/>Edit</Link><Link href={`/sets/${slug}/compare`} className="flex items-center justify-center gap-2 rounded-xl border p-2.5 text-sm font-bold">Compare</Link></div><button onClick={()=>request(`/sets/${slug}`,"PATCH",{status:set.status==="ARCHIVED"?"DRAFT":"ARCHIVED"},set.status==="ARCHIVED"?"Set restored to drafts.":"Set archived.")} className="mt-2 w-full rounded-xl border p-2.5 text-sm font-bold">{set.status==="ARCHIVED"?"Restore set":"Archive set"}</button></div>:<div className="panel grid grid-cols-2 gap-2 p-3"><button disabled={Boolean(busy)} onClick={()=>request(`/sets/${slug}/follow`,viewer.isFollowing?"DELETE":"POST",undefined,viewer.isFollowing?"Set unfollowed.":"Set followed.")} className={`flex items-center justify-center gap-2 rounded-xl p-3 text-sm font-bold ${viewer.isFollowing?"border":"bg-indigo-600 text-white"}`}><Heart size={17} fill={viewer.isFollowing?"currentColor":"none"}/>{viewer.isFollowing?"Unfollow":"Follow"}</button><button onClick={async()=>{await navigator.clipboard.writeText(location.href);setNotice("Share link copied.")}} className="flex items-center justify-center gap-2 rounded-xl border p-3 text-sm font-bold"><Share2 size={17}/>Share</button><button onClick={()=>request(`/sets/${slug}/copy`,"POST",undefined,"Private copy created.").then(result=>result?.set&&router.push(`/sets/${result.set.slug}`))} className="flex items-center justify-center gap-2 rounded-xl border p-3 text-sm font-bold"><Copy size={17}/>Copy</button><button disabled={set.visibility!=="PUBLIC"} onClick={()=>request(`/sets/${slug}/fork`,"POST",undefined,"Private fork created with attribution.").then(result=>result?.set&&router.push(`/sets/${result.set.slug}`))} className="flex items-center justify-center gap-2 rounded-xl border p-3 text-sm font-bold disabled:opacity-40"><Forklift size={17}/>Fork</button></div>}
        <div className="rounded-xl bg-amber-50 p-4 text-xs text-amber-900 dark:bg-amber-950 dark:text-amber-100"><TriangleAlert className="mb-2" size={18}/>Demo prices are not live offers. “Likely” compatibility still requires checking model ports, load and dimensions.</div>
      </aside>
    </div>
    {data.similar.length>0&&<section><p className="eyebrow">Why these match</p><h2 className="mt-2 text-2xl font-black">Similar sets you may find useful</h2><div className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{data.similar.map((value:any)=><div key={value.id}><SetCard set={value}/><p className="mt-2 px-2 text-xs font-semibold text-indigo-600">{value.similarityReason}</p></div>)}</div></section>}
  </div>;
}

function Stat({label,value}:{label:string;value:string}){return <div><small className="text-slate-400">{label}</small><strong className="mt-1 block text-xl">{value}</strong></div>}
