import Link, {useParams} from "@/lib/navigation";
export default function Fork(){const {slug}=useParams<{slug:string}>();return <div className="mx-auto max-w-2xl panel p-8"><h1 className="mt-3 text-3xl font-black">Make this set yours</h1><p className="mt-4 text-slate-500">Forking creates a private draft, copies its functional needs, and permanently retains the original creator and set slug in every copied item’s attribution.</p><Link href={`/sets/${slug}`} className="btn-primary mt-6 inline-block">Review and fork</Link></div>}
