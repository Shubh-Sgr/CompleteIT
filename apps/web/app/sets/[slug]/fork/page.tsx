import Link from "next/link";
export default async function Fork({params}:{params:Promise<{slug:string}>}){const {slug}=await params;return <div className="mx-auto max-w-2xl panel p-8"><p className="eyebrow">Fork safely</p><h1 className="mt-3 text-3xl font-black">Make this set yours</h1><p className="mt-4 text-slate-500">Forking creates a private draft, copies its functional needs, and permanently retains the original creator and set slug in every copied item’s attribution.</p><Link href={`/sets/${slug}`} className="mt-6 inline-block rounded-xl bg-indigo-600 px-4 py-2.5 font-bold text-white">Review and fork</Link></div>}
