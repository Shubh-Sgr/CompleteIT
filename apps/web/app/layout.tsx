import type {Metadata} from "next";import "./globals.css";import {Providers} from "@/components/providers";import {Nav} from "@/components/nav";
export const metadata:Metadata={title:{default:"CompleteIt — Complete anything","template":"%s · CompleteIt"},description:"Turn anything you own or plan to add into a complete, useful set without being limited to one product category.",manifest:"/manifest.webmanifest"};
export default function Layout({children}:{children:React.ReactNode}){return <html lang="en" suppressHydrationWarning><body><Providers><Nav/><main className="mx-auto min-h-[calc(100vh-4rem)] max-w-7xl px-4 py-6 pb-24 sm:py-10 md:pb-10">{children}</main></Providers></body></html>}
