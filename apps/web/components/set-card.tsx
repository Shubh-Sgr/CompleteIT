import Link from "next/link";import {ArrowRight,CheckCircle2,IndianRupee,Star,Users} from "lucide-react";import {money} from "@/lib/api";
export function SetCard({set}:{set:any}){return <Link href={`/sets/${set.slug}`} className="panel group overflow-hidden transition hover:-translate-y-1 hover:shadow-soft"><div className="relative h-36 bg-gradient-to-br from-indigo-100 via-violet-50 to-amber-100 p-5 dark:from-indigo-950 dark:via-slate-900 dark:to-amber-950"><span className="chip">{set.setType?.toLowerCase().replace("_"," ")??"community"}</span><div className="absolute bottom-4 right-4 grid size-14 place-items-center rounded-2xl bg-white/80 text-3xl shadow-sm backdrop-blur dark:bg-black/30" aria-hidden>🧩</div></div><div className="p-5"><div className="mb-2 flex items-start justify-between gap-3"><h3 className="font-bold leading-snug group-hover:text-indigo-600">{set.title}</h3><ArrowRight className="mt-0.5 shrink-0 opacity-0 transition group-hover:translate-x-1 group-hover:opacity-100" size={17}/></div><p className="mb-4 line-clamp-2 text-sm text-slate-500 dark:text-slate-400">{set.description}</p><div className="flex flex-wrap gap-2 text-xs text-slate-600 dark:text-slate-300"><span className="flex items-center gap-1"><IndianRupee size={13}/>{money(set.totalPrice??set.budget)}</span><span className="flex items-center gap-1"><CheckCircle2 size={13}/>{Math.round(set.slotScore??0)}% slots</span><span className="flex items-center gap-1"><Star size={13}/>{Number(set.communityRating??0).toFixed(1)}</span>{set._count&&<span className="flex items-center gap-1"><Users size={13}/>{set._count.followers}</span>}</div></div></Link>}
